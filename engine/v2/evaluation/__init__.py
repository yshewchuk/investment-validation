"""Evaluation/portfolio realized outcomes, capital accounting, report generation

Layer 6 of `system_rearchitecture.md` §4.1. Replaces `evaluate.py`, `report.py`, `build_trades.py`, `calibrate.py`, `recalibrate.py`.

Empty by construction: phase 0 writes no production logic
(`guides/rearchitecture_phase0_baseline.md` §10). See ``README.md`` for what
this package will own, what it deliberately will not, and which packages may
import it.
"""
