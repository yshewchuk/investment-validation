"""paths, env, canonical JSON, session/calendar arithmetic, causality primitives

Layer 0 of `system_rearchitecture.md` §4.1. Replaces `paths.py`, `env.py`, `jsonio.py`, `audit.py`, `session arithmetic from calendar.py`.

Empty by construction: phase 0 writes no production logic
(`guides/rearchitecture_phase0_baseline.md` §10). See ``README.md`` for what
this package will own, what it deliberately will not, and which packages may
import it.
"""
