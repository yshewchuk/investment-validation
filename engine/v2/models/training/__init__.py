"""Model training dataset and model recipes, folds, fitting, evidence, release candidates

Layer 6 of `system_rearchitecture.md` §4.1. Replaces `models/training/, rewritten above scoring rather than moved`.

Empty by construction: phase 0 writes no production logic
(`guides/rearchitecture_phase0_baseline.md` §10). See ``README.md`` for what
this package will own, what it deliberately will not, and which packages may
import it.
"""
