# Secret scanning / push protection exclusions.
# Only this file: a redaction test that historically contained a made-up
# Stripe-shaped key (commit 17d67d4). The test now builds its fake secret at
# runtime; see AGENTS.md "Fake credentials in tests". Do not widen this list.
paths-ignore:
  - "tests/test_v2_ops_explain_from_logs.py"
